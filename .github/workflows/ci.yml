---
name: CI
'on':
  pull_request:
  push:
    branches:
      - main

jobs:
  get-version:
    uses: ./.github/workflows/getVersion.yml@main
    with:
      metadataFile: 'metadata.json'
      metadataPath: .
  yaml-lint:
    uses: ./.github/workflows/yamlLint.yml@main
    with:
      yamlPath: '.github'
    needs: get-version
  update-version:
    uses: ./.github/workflows/updateVersion.yml@main
    with:
      gitUsername: 'grababyte'
      gitEmail: 'grababyte@github.com'
      metadataFile: 'metadata.json'
      metadataPath: .
    needs: yaml-lint
