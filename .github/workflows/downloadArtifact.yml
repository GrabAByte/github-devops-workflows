---
name: Download Artifact
'on':
  workflow_call:
    inputs:
      artifact:
        required: true
        type: string

job:
  downloadArtifact:
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v1
        with:
          name: ${{ inputs.artifact }}
